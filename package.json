{
  "name": "copsidian",
  "version": "0.0.6",
  "description": "Embed the complete OpenCode agent inside Obsidian sidebar",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit && node esbuild.config.mjs --minify",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.21.1",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.28.0",
    "obsidian": "latest",
    "typescript": "^5.7.0",
    "vitest": "^4.1.7"
  },
  "author": "YouSheng Kang",
  "license": "MIT",
  "private": true
}
